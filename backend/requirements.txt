fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
pillow>=10.0.0
numpy>=1.24.0
requests>=2.31.0

# Real-ESRGAN AI inference — CPU-only wheel (no CUDA drivers needed)
# Render installs this directly from PyTorch CPU wheel repo
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
